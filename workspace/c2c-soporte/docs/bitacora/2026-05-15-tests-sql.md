# Bitacora - Tests SQL

## Fecha

2026-05-15

## Objetivo

Agregar pruebas automatizadas para el validador SQL y el resumen de planes antes de conectar con PostgreSQL real.

## Cambios

- Agregar script `npm test`.
- Crear tests para `validateReadOnlySql`.
- Crear tests para `summarizePlan`.
- Crear runner `src/test.ts`.

## Casos cubiertos

- SELECT valido.
- WITH valido.
- Rechazo de sentencia no lectura.
- Rechazo de multiples sentencias.
- Rechazo de comentarios SQL.
- Rechazo de comandos peligrosos dentro de CTE.
- Deteccion de `Seq Scan`.
- Plan sin `Seq Scan`.
- Entrada desconocida sin falla.

## Ajuste tecnico

Se evito `tsx --test` porque en esta sesion de Windows fallo con `spawn EPERM`. El script final compila con TypeScript y ejecuta `node dist/test.js`, evitando procesos auxiliares de transformacion en caliente.

## Verificacion ejecutada

- `npm test`: 9 tests OK.
- `npm run typecheck`: OK.
- `npm run build`: incluido dentro de `npm test`.
